# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/
.eggs/
*.so

# ── Virtual environments ──
venv/
.venv/
env/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Swift / Xcode ──
.build/
.swiftpm/
DerivedData/
xcuserdata/
*.xcuserdata/

# ── Backend runtime data ──
backend/app.db
backend/app.db-*

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Data (raw NHANES XPT files, too large for git) ──
data/raw/
data/processed/*.parquet

# ── Trained models and SHAP arrays ──
results/models/
results/*.npy
results/figures/

# ── LaTeX build artifacts ──
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/*.synctex(busy)

# ── Compiled PDF (regenerate via pdflatex) ──
paper/*.pdf

# ── Temporary fix scripts ──
fix_*.py
fix_subgroup2.py

# ── Jupyter ──
.ipynb_checkpoints/

# ── Misc ──
*.bak
*.tmp
